# Render.com Configuration
services:
  - type: web
    name: ecovillage-map
    env: node
    buildCommand: echo "No build required"
    startCommand: node server-complete.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
